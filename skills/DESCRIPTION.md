---
description: Super Data Analytics 工作流技能——查询、语义检索、异动诊断、预测、效果评估、可视化、报告与编排，经本机 sda MCP 服务执行。
---

本分类通过符号链接 `~/.hermes/skills/super-data-analytics -> ~/sda-mcp/skills` 提供，
`git pull` 即生效，无需手动同步。`README.md` 面向仓库维护者，Hermes 扫描器只认子目录中的 `SKILL.md`。
