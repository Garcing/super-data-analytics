# 公司 OpenVPN 客户端配置（容器版）
# 由 2020 年 Windows 版 client.ovpn 改造：
#   - ca.crt 是公开信任锚，随镜像内置；ta.key 是机密，运行时挂载 runtime/
#   - 删除 Windows 专用指令 route-method exe / route-delay 2
#   - 账密由 entrypoint 从环境变量写入 /run/pass.txt（config.json -> compose 注入）
#   - data-ciphers-fallback / auth SHA1：兼容 2.4 时代服务端
client
dev tun
proto udp
remote 47.99.47.121 2294
resolv-retry infinite
nobind
persist-key
persist-tun
keepalive 10 120
cipher AES-256-CBC
data-ciphers-fallback AES-256-CBC
auth SHA1
comp-lzo
verb 3
ca /etc/openvpn/ca.crt
tls-auth /etc/openvpn/runtime/ta.key 1
auth-user-pass /run/pass.txt
